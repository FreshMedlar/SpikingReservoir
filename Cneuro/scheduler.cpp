#include "scheduler.h"
#include "global.h"

Scheduler::Scheduler() {
    // toReceive.clear();
    // toReceive.reserve(size);  // Reserve memory to optimize performance
    // for (int i = 0; i < size; i++) {
    //     toReceive[i] = neurons[i];  // Create Neuron with ID = i
    // }
}

